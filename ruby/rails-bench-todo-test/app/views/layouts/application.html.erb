<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails Bench Todo Test" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap 5 CSS %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <%# FontAwesome %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      body {
        background-color: #f8f9fa;
      }
      .sidebar {
        height: 100vh;
        position: fixed;
        top: 0;
        left: 0;
        padding-top: 1rem;
        background-color: #fff;
        border-right: 1px solid #dee2e6;
        z-index: 100;
      }
      .main-content {
        margin-left: 16.66667%; /* Corresponds to col-md-2 */
        padding: 2rem;
      }
      @media (max-width: 768px) {
        .sidebar {
          position: static;
          height: auto;
          border-right: none;
          border-bottom: 1px solid #dee2e6;
        }
        .main-content {
          margin-left: 0;
        }
      }
      .todo-card {
        transition: transform 0.2s;
      }
      .todo-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 .5rem 1rem rgba(0,0,0,.15)!important;
      }
    </style>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
        <!-- Sidebar -->
        <nav class="col-md-2 d-md-block sidebar collapse show">
          <div class="position-sticky">
            <div class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-decoration-none px-3">
              <span class="fs-4"><i class="fas fa-check-double text-primary"></i> TodoApp</span>
            </div>
            <hr>
            <ul class="nav flex-column">
              <li class="nav-item">
                <%= link_to root_path, class: "nav-link active" do %>
                  <i class="fas fa-home me-2"></i> Dashboard
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to todos_path, class: "nav-link link-dark" do %>
                  <i class="fas fa-list me-2"></i> All Todos
                <% end %>
              </li>
              <li class="nav-item">
                <a href="#" class="nav-link link-dark">
                  <i class="fas fa-calendar me-2"></i> Calendar
                </a>
              </li>
            </ul>
            
            <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted text-uppercase">
              <span>Projects</span>
              <a class="link-secondary" href="<%= new_project_path %>" aria-label="Add a new project">
                <i class="fas fa-plus-circle"></i>
              </a>
            </h6>
            <ul class="nav flex-column mb-2" id="sidebar-projects">
               <%# Projects will be loaded here %>
               <% Project.limit(5).each do |project| %>
                 <li class="nav-item">
                   <%= link_to project_path(project), class: "nav-link link-dark" do %>
                     <i class="fas fa-folder me-2"></i> <%= project.name %>
                   <% end %>
                 </li>
               <% end %>
            </ul>
          </div>
        </nav>

        <!-- Main Content -->
        <main class="col-md-10 ms-sm-auto main-content">
          <%= yield %>
        </main>
      </div>
    </div>

    <%# Bootstrap JS %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  </body>
</html>
