{
  "name": "react-clone-monorepo",
  "version": "1.0.0",
  "description": "A React-like SPA library monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "clean": "turbo run clean",
    "example": "turbo run dev --filter=@react-clone/todo-app",
    "core:build": "turbo run build --filter=@react-clone/core",
    "core:dev": "turbo run dev --filter=@react-clone/core"
  },
  "keywords": ["react", "spa", "virtual-dom", "components", "monorepo"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@10.12.1"
}
